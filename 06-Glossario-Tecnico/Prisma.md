# Prisma ORM 🧩

**O que é:**
ORM para TypeScript com suporte a Postgres, gerando tipos e migrations.

**Por que importa aqui:**
Padroniza acesso a dados em backends NestJS.

**Exemplo:**
- Schemas `.prisma`, migrations e client tipado.

**Como pedir para a IA:**
> "Integrar Prisma ao `obm-auth` com Postgres e migrations automáticas."