# Starter: FastAPI Service (IA)

## Conteúdo
- API FastAPI com estrutura mínima.
- Dockerfile e docker-compose.
- `requirements.txt` com dependências básicas.

## Como usar
- Copie a pasta; ajuste `.env`.
- `docker-compose up -d`.
- `uvicorn app:app --reload`.